# Normalize to LF in the repository so the Linux CI runner that builds the site
# and Windows development agree on file contents.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
