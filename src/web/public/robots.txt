# Everything here is meant to be found.
User-agent: *
Allow: /

# The API answers JSON the pages already render; there is nothing for a crawler
# to index behind it, and every URL under it is a wasted request.
Disallow: /api/
